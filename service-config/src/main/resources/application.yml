spring:
 application:
    name: service-config
 cloud:
    config:
      server:
        git:
          uri: https://github.com/raj4rr/config-server.git
server:
  port: 9196
eureka:
  client:
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
instance:
    hostname: localhost